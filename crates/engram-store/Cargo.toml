[package]
name = "engram-store"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = "1"
serde_json = "1"
engram-types = { path = "../engram-types" }
